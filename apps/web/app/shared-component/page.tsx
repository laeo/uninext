import { VideoCard } from 'ui';

export default function SharedComponentPage() {
    return (
        <main className=' flex flex-col items-center justify-center min-h-screen p-4'>
            <VideoCard src={''}></VideoCard>
        </main>
    );
}